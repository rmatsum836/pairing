package:
  name: pairing
  version: "0.0.0"

source:
  path: ../..

build:
  number: 0

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - numpy
    - mdtraj
    - mtools

test:
  requires:
    - pytest
  imports:
    - pairing

about:
  home: add_url_here
  license: MIT License
